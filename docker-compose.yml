version: '3.8'
services:
  # Development service
  dev:
    build: .
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_ENV=development
      # - VITE_SERVER_HOST=0.0.0.0
    command: pnpm dev --host
